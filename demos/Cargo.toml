[workspace]

resolver = "2"
members = [
    "cairo",
    "cairo_shadow_button",
    "calculator",
    "csv",
    "dialect",
    "fltodo",
    "flightbooker",
    "resters",
    "sudokusolver",
]

[profile.release]
strip = true
opt-level = "z"
lto = true
panic = "abort"
