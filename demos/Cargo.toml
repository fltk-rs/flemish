[workspace]

resolver = "2"
members = [
    "cairo",
    "cairo_shadow_button",
    "csv",
    "fltodo",
    "flightbooker",
]

[profile.release]
strip = true
opt-level = "z"
lto = true
panic = "abort"
