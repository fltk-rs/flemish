[workspace]

resolver = "2"
members = [
    "base64",
    "cairo",
    "cairo_shadow_button",
    "calculator",
    "csv",
    "dialect",
    "flightbooker",
    "fltodo",
    "picture",
    "resters",
    "sudokusolver",
]

[profile.release]
strip = true
opt-level = "z"
lto = true
panic = "abort"
